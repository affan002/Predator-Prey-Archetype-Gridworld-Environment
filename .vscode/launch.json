{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run GridWorld Test",
      "type": "python",
      "request": "launch",
      // Use -m module form instead of running the file directly
      "module": "single_agent_package.tests.test_gridworld",
      // Point cwd at your env_src so that single_agent_package is a top-level package
      "cwd": "${workspaceFolder}/codebase/src",
      "justMyCode": true
    }
  ]
}
